# Ideas & Notes

Use this file to jot quick ideas and decisions; keep entries short.

## 2025-09-24
- Utility: `slugify()` implemented with tests; room for `strict=True` mode to restrict to `[a-z0-9-]` postconditions.
- Possible CLI: `python -m chaos "Your Title"` -> prints slug.
- More string utils: `kebab_to_title`, `camel_to_snake`, `truncate_words(n)`.
- Process: enforce PR template checklist; CI requires CHANGELOG update when code changes.

